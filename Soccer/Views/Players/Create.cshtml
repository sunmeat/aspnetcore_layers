@model Soccer.BLL.DTO.PlayerDTO

@{
    ViewBag.Title = "Створення гравця";
}

<h2>Додавання нового гравця</h2>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                @Html.Label("Гравець:")
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                @Html.Label("Вік гравця:")
                <input asp-for="Age" class="form-control" />
                <span asp-validation-for="Age" class="text-danger"></span>
            </div>
            <div class="form-group">
                @Html.Label("Позиція на полі:")
                <input asp-for="Position" class="form-control" />
                <span asp-validation-for="Position" class="text-danger"></span>
            </div>
            <div class="form-group">
                @Html.Label("Клуб:")
                <select asp-for="TeamId" class="form-control" asp-items="ViewBag.ListTeams as SelectList"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Додати гравця" class="btn btn-primary" style="margin-top: 20px; margin-bottom: 20px;" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">До списку гравців</a>
</div>
@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}